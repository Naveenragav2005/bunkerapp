specVersion: "v1beta"
serviceId: "fuel"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fuel-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
