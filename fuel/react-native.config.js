module.exports = {
    dependencies: {
      'react-native-svg': {
        platforms: {
          android: null, // if using expo or to prevent native linking
          ios: null,     // if using expo or to prevent native linking
        },
      },
    },
  };
  